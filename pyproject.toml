[project]
name = "oem-mcp"
version = "0.9.5"
description = "Monorepo for OpenEmpiric OEM MCP workspace"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "packages/*",
]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib"

