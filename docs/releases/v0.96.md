# OpenEmpiric v0.96

## Agent-First Runtime

OEM no longer relies on agents to manage memory lifecycle.

Session restoration, reflection, recovery, and outcome recording are handled automatically by the runtime.

Agents receive project memory as context and may optionally use search and health capabilities when needed.
